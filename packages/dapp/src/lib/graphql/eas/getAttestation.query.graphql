query GetAttestation($where: AttestationWhereUniqueInput!) {
  attestation(where: $where) {
    id
    attester
    recipient
    decodedDataJson
    timeCreated
    revoked
    revocationTime
    txid
  }
}
